import 'package:flutter/material.dart';

class DarButton extends StatelessWidget {
  const DarButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: null,
    );
  }
}
