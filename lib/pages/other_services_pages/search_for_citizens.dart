import 'package:flutter/material.dart';

class SearchForCitizens extends StatelessWidget {
  const SearchForCitizens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Text('SearchForCitizens'),
      ),
    );
  }
}
